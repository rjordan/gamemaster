
<div id="content">
<h1><%= @user.nickname %></h1>
<p>
</p>
<table class="show_form">
  <tr>
    <td class="show_label">Signup Date:</td>
    <td class="show_value"><%= @user.created_at.to_s(:cst) %></td>
  </tr>
  </table>
<br/>

<!-- either an email or a nickname -->
<div id="section_heading">Running Campaigns<%= link_to "New Campaign", new_campaign_url -%></div>
<hr/>
<table class="show_form">
<% if @user.campaigns %>
<% for campaign in @user.campaigns %>
  <tr>
    <td><%= link_to campaign.name, campaign_url(campaign) %></td>
    <td><%= campaign.system.name %></td>
  </tr>
<% end %>
<% end %>
  </table>
<br/>

</div>

<%= link_to_if (@user==session[:user_id]), 'Edit', edit_user_url(@user) %> 
